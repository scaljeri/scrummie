taskStats = function () {
    // find all active sprints

    // find all tasks per sprint

    // count tasks per lane + total

    // find closed sprint between now and last run
    // count FTE or number of tasks
};